# Telegram SEO Bot Mini App

SEO-ассистент на Claude с интеграцией Just-Magic API.

## Что умеет

- 🔍 **Кластеризация семантики** — группировка запросов по топам выдачи
- 📊 **Сбор частотности** — данные из Яндекс.Wordstat
- 💡 **Парсинг подсказок** — сбор поисковых подсказок с итерациями
- 📝 **Текстовый анализ** — проверка оптимизации страниц
- 🎨 **LSI-анализ** — проверка тематичности текста (Акварель)
- 📑 **Тематическая классификация** — определение категорий запросов
- И многое другое...

## Быстрый старт

### 1. Создать Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Придумайте имя и username
4. Сохраните токен бота

### 2. Получить Anthropic API Key

1. Зарегистрируйтесь на [console.anthropic.com](https://console.anthropic.com)
2. Создайте API ключ
3. Сохраните его

### 3. Деплой на Railway

#### Вариант А: Через GitHub

1. Форкните/загрузите этот репозиторий на GitHub
2. Зайдите на [railway.app](https://railway.app)
3. New Project → Deploy from GitHub repo
4. Выберите репозиторий
5. Добавьте переменные окружения:
   - `ANTHROPIC_API_KEY` — ваш ключ Anthropic
   - `JUSTMAGIC_API_KEY` — `d691fe469cf2cded4b1aca5b26b582cd`
   - `TELEGRAM_BOT_TOKEN` — токен от BotFather
6. Railway автоматически задеплоит приложение
7. Скопируйте URL (например: `https://your-app.up.railway.app`)

#### Вариант Б: Через Railway CLI

```bash
# Установить CLI
npm install -g @railway/cli

# Логин
railway login

# Создать проект
railway init

# Добавить переменные
railway variables set ANTHROPIC_API_KEY=sk-ant-xxx
railway variables set JUSTMAGIC_API_KEY=d691fe469cf2cded4b1aca5b26b582cd
railway variables set TELEGRAM_BOT_TOKEN=xxx

# Деплой
railway up
```

### 4. Настроить Mini App в Telegram

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Bot Settings → Menu Button → Configure menu button
5. Введите URL: `https://your-app.up.railway.app`
6. Введите текст кнопки: `SEO Assistant`

Или через команды:
```
/setmenubutton
```
Выберите бота и введите URL.

### 5. Готово!

Откройте бота в Telegram и нажмите кнопку меню — откроется Mini App!

## Локальная разработка

```bash
cd backend

# Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установить зависимости
pip install -r requirements.txt

# Создать .env файл
cp .env.example .env
# Заполните переменные в .env

# Запустить
uvicorn main:app --reload --port 8000
```

Откройте http://localhost:8000 для тестирования интерфейса.

## Структура проекта

```
backend/
├── main.py              # FastAPI приложение
├── justmagic_tools.py   # Инструменты Just-Magic
├── static/
│   └── index.html       # Telegram Mini App UI
├── requirements.txt     # Зависимости
├── Dockerfile          # Для контейнеризации
├── railway.toml        # Конфиг Railway
└── .env.example        # Пример переменных
```

## API Endpoints

- `GET /` — Mini App интерфейс
- `GET /health` — Health check
- `POST /api/chat` — Основной чат с Claude
- `POST /api/clear` — Очистка истории
- `GET /api/tasks` — Список задач Just-Magic
- `GET /api/tasks/{tid}` — Информация о задаче
- `GET /api/account` — Информация об аккаунте

## Примеры использования

### Кластеризация запросов
> "Кластеризуй запросы: купить iPhone, iPhone цена, iPhone 15 Pro, смартфон Apple"

### Сбор частотности
> "Собери частотность для запросов: пластиковые окна, окна ПВХ, заказать окна"

### Анализ страницы
> "Проанализируй страницу https://example.com по запросам: купить ноутбук, ноутбук цена"

### Проверка задачи
> "Покажи результат задачи 12345"

## Troubleshooting

### Mini App не открывается
- Убедитесь, что URL корректный и приложение запущено
- Проверьте, что URL начинается с `https://`

### Ошибка "AI service error"
- Проверьте ANTHROPIC_API_KEY
- Убедитесь, что на аккаунте есть баланс

### Инструменты не работают
- Проверьте JUSTMAGIC_API_KEY
- Посмотрите логи в Railway

## Дальнейшее развитие

- [ ] Добавить Google Search Console интеграцию
- [ ] Webhooks для уведомлений о готовности задач
- [ ] Сохранение истории в базу данных
- [ ] Авторизация пользователей
- [ ] Ограничение запросов

## Лицензия

MIT
